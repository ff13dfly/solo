{
  "id": "wf_footprint_branch_test",
  "category": "process",
  "name": "Footprint pre-check branching test workflow",
  "desc": "Step s2 only runs when s1 result flag is true. Used to verify that even branch-conditional methods are included in the footprint (all-or-nothing pre-check).",
  "required_inputs": [],
  "steps": [
    {
      "id": "s1",
      "service": "ledger",
      "method": "ledger.transfer",
      "params": { "amount": 100 }
    },
    {
      "id": "s2",
      "service": "bank",
      "method": "bank.dispatch",
      "condition": { "===": [{ "var": "step.s1.result.approved" }, true] },
      "params": { "ref": "abc" }
    }
  ]
}
